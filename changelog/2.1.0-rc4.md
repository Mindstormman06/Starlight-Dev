AINB Editor:
- Fixed Bool input parameters (checkboxes) always loading unticked regardless of the actual saved value
- Added a custom .nlyt format for modders to save their hand-sorted nodes, and lazy load them if they close the software.

Map Editor:
- Fixed custom actors and project-specific map changes not being read for projects created before the romfs subfolder restructure
- Added a camera speed dropdown to the viewport toolbar
- Moved the Play button to a discreet corner of the viewport

Terrain:
- Fixed high-LOD terrain areas that share an exact edge with a section boundary being silently skipped, causing seemingly random patches of terrain to be unsculptable and fail to export correctly